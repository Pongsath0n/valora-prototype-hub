import AppLayout from "@/components/AppLayout";
import { useRoleGuard } from "@/lib/guards";
const events=["user_login","product_created","product_updated","ingredient_updated","stock_adjusted","order_created","order_status_changed","payment_confirmed","stock_deducted","channel_fee_updated","image_uploaded"];
export default function AdminAuditLogsPage(){const {checking,accessDenied}=useRoleGuard(["owner","admin"]);if(checking)return <div className="min-h-screen flex items-center justify-center">กำลังโหลด...</div>;if(accessDenied)return <div className="min-h-screen flex items-center justify-center text-xl font-semibold">Access Denied</div>;return <AppLayout><div className="space-y-4"><h1 className="page-title">บันทึกเหตุการณ์ระบบ</h1><div className="stat-card"><p className="text-sm text-muted-foreground mb-3">หน้านี้จะใช้สำหรับดูบันทึกเหตุการณ์สำคัญของระบบ เช่น การสร้างออเดอร์ การเปลี่ยนสถานะออเดอร์ การแก้ไขเมนู การปรับสต็อก และการตั้งค่าช่องทางขาย โดยไม่ใช่ระบบวิเคราะห์ Traffic ทางการตลาด</p><table className="data-table"><thead><tr><th>event</th><th>status</th></tr></thead><tbody>{events.map(e=><tr key={e}><td>{e}</td><td>placeholder</td></tr>)}</tbody></table></div></div></AppLayout>}
